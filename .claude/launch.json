{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "cd backend && uv run uvicorn main:app --host 0.0.0.0 --port $PORT --reload --reload-dir ."],
      "port": 8000,
      "autoPort": true
    }
  ]
}
