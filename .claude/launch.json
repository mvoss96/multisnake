{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "backend/venv/bin/uvicorn main:app --app-dir backend --host 0.0.0.0 --port $PORT --reload --reload-dir backend"],
      "port": 8000,
      "autoPort": true
    }
  ]
}
